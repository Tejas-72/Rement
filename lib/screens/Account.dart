import 'package:flutter/material.dart';
class UsrAccount extends StatelessWidget {
  const UsrAccount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:  Center(child: Text('Account')),
    );
  }
}